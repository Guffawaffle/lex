# Example Prompt

This is an example prompt template for testing the canon/ build system.

You can customize prompts by:
1. Creating a local override in `.smartergpt.local/prompts/`
2. Setting `LEX_CANON_DIR` environment variable

## Usage

This prompt demonstrates the precedence chain for prompt loading.
