---
"@smartergpt/lex": minor
---

Add an opt-in PostgreSQL FrameStore shared by Lex CLI and MCP, with backend-neutral introspection, cross-store contract coverage, and safe PostgreSQL capability handling. Also make frame export pagination and metadata lossless, prevent SQLite backup collisions, and report partial import dry-run failures correctly.
